# Copy this file to .env. The copied file is ignored by Git.
# Never put real keys in this committed example.
# scripts/Start-InboxPilot.ps1 loads non-empty values into the child process
# without printing them. Direct `uv run` commands use the current shell environment.
OPENAI_API_KEY=
DEEPSEEK_API_KEY=
