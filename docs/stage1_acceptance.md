# 阶段一验收报告

## 基本信息

- 项目：InboxPilot
- 阶段：阶段一——离线 MVP
- 验收日期：2026-08-07
- 验收结论：全部通过
- 暂缓项：无

本轮按照 README 中的阶段一验收标准执行。自动化质量、离线功能、隐私检查和全新隔离环境安装验证均已通过。

## 自动化质量结果

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| 全新环境安装 | `uv sync --locked` | 在独立工作目录和全新虚拟环境中安装成功 |
| 全新环境冒烟测试 | `uv run inbox-agent demo` | 成功处理 20 封邮件，失败 0 |
| 单元测试与分支覆盖率 | `uv run pytest --cov=src/inbox_agent --cov-report=term-missing` | 90 项通过，总覆盖率 96.10% |
| 代码规范 | `uv run ruff check .` | 通过 |
| 代码格式 | `uv run ruff format --check .` | 通过 |
| 静态类型 | `uv run mypy src` | 通过 |
| Git 补丁格式 | `git diff --check` | 通过 |

`pyproject.toml` 已将覆盖率失败门槛配置为 80%，后续执行覆盖率命令时，低于该门槛将返回失败状态。

## 功能验收

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| CLI 不依赖邮箱或 API Key | 通过 | `inbox-agent demo` 在离线环境成功处理 20 封邮件 |
| 匿名样例数量 | 通过 | `data/samples/sample_emails.json` 包含 20 封虚构邮件 |
| 输入数据校验 | 通过 | Pydantic 模型及加载器测试覆盖缺失字段、非法 JSON、非法时间和重复 ID |
| HTML 清理 | 通过 | 正文清理测试覆盖 HTML、脚本、样式、空白和签名处理 |
| 配置与代码分离 | 通过 | 规则位于 `config/rules.yaml`，由规则加载器读取并校验 |
| 可解释评分 | 通过 | 每次分数变化输出原因代码、说明、变化值及匹配内容 |
| 优先级与分数范围 | 通过 | 输出覆盖 P1～P5，分数限定在 0～100 |
| 重要群发邮件保护 | 通过 | 考试变更、安全警报和课程取消等群发案例保持高优先级 |
| 表格与 JSON 输出 | 通过 | CLI 测试覆盖两种输出形式及机器可读 JSON |
| 离线回归评测 | 通过 | 20 个预测与独立标签一致，评测结果为 PASS |

本次匿名回归数据集上的优先级准确率、类别准确率、复核一致率、P1 精确率和 P1 召回率均为 100%。这组结果用于防止规则回归，不代表系统在真实邮箱中的泛化准确率。

## 隐私与仓库检查

- 样例邮箱使用 `example.edu` 或 `example.com` 保留域名。
- 样例人物、学校和邮件内容均为虚构数据。
- 未发现邮箱密码、API Key、访问令牌、刷新令牌或客户端密钥。
- `.env`、令牌缓存、私有数据目录、日志、虚拟环境和测试缓存均已加入 `.gitignore`。
- 阶段一不连接 Microsoft 365，不读取或修改真实邮箱。

## 全新环境验证

项目已在同一台电脑上的独立 Git Worktree、全新 `.venv` 和隔离依赖缓存中执行：

```powershell
uv sync --locked
uv run inbox-agent demo
uv run inbox-agent evaluate
uv run pytest
```

安装、CLI 冒烟测试、离线评测和自动化测试全部通过。测试 Worktree、虚拟环境和隔离缓存已在验证后删除，不依赖原项目的 `.venv`。

## 最终结论

阶段一离线 MVP 的全部验收项已于 2026-08-07 完成。项目可以进入结构化 LLM 与 Microsoft Graph 集成等后续阶段。
